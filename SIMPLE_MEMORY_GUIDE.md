# 简单记忆功能使用指南

## 🎯 功能概述

现在使用简化的内存记忆功能，不依赖复杂的Spring AI记忆组件，提供稳定可靠的对话记忆能力。

## ✨ 主要特点

- ✅ **简单可靠** - 使用内存存储，无需复杂配置
- ✅ **自动记忆** - 每次对话都会自动保存历史
- ✅ **上下文理解** - AI能够记住之前的对话内容
- ✅ **会话管理** - 支持多会话隔离
- ✅ **历史查看** - 可以查看完整的对话历史
- ✅ **会话清除** - 支持清除特定会话的历史

## 🚀 快速开始

### 1. 启动后端服务

```bash
cd chatbotApi
mvn clean install
mvn spring-boot:run
```

### 2. 启动前端服务

```bash
npm run dev
```

### 3. 测试记忆功能

1. 打开浏览器访问：http://localhost:5173
2. 发送消息："你好，我是小源，请记住我"
3. 发送消息："你还记得我是谁吗？"
4. 点击"查看历史"按钮查看对话记录

## 🔧 技术实现

### 后端实现

- **SimpleMemoryService** - 简单记忆服务
- **MemoryController** - 记忆API控制器
- **内存存储** - 使用ConcurrentHashMap存储会话历史

### 前端实现

- **会话管理** - 自动生成和管理会话ID
- **历史查看** - 弹窗显示对话历史
- **会话控制** - 支持新会话和清除历史

## 📝 API端点

- `GET /chat/simple` - 简单聊天
- `GET /chat/stream` - 流式聊天
- `POST /chat/stream` - POST流式聊天
- `GET /chat/messages` - 获取历史消息
- `DELETE /chat/clear/{conversationId}` - 清除会话历史
- `GET /chat/test` - 测试接口

## 🎮 使用方法

### 基本对话

直接发送消息，AI会自动记住对话内容：

```
用户：你好，我是张三
AI：你好张三！很高兴认识你。

用户：你还记得我是谁吗？
AI：当然记得！你是张三，我们刚才已经认识了。
```

### 查看历史

点击"查看历史"按钮可以查看当前会话的所有消息。

### 新会话

点击"新会话"按钮开始一个全新的对话，之前的记忆不会影响新会话。

### 清除历史

点击"清除历史"按钮删除当前会话的所有历史记录。

## 🔍 测试步骤

### 1. 基本记忆测试

```bash
# 第一次对话
curl "http://localhost:5649/chat/simple?query=我是小源&conversation_id=test1"

# 第二次对话
curl "http://localhost:5649/chat/simple?query=你还记得我是谁吗&conversation_id=test1"
```

### 2. 历史查询测试

```bash
curl "http://localhost:5649/chat/messages?conversation_id=test1"
```

### 3. 清除会话测试

```bash
curl -X DELETE "http://localhost:5649/chat/clear/test1"
```

## 💡 优势

相比复杂的Spring AI记忆组件：

1. **更简单** - 无需复杂的数据库配置
2. **更稳定** - 不依赖外部组件
3. **更快速** - 内存存储，响应更快
4. **更易调试** - 代码简单，容易理解和修改

## ⚠️ 注意事项

1. **内存限制** - 历史记录存储在内存中，重启服务会丢失
2. **会话隔离** - 不同会话ID的对话互不影响
3. **历史限制** - 每个会话最多保留20条消息
4. **并发安全** - 使用ConcurrentHashMap确保线程安全

## 🎉 总结

现在您的聊天机器人拥有了简单而有效的记忆功能！AI可以记住对话历史，提供连贯的对话体验，同时保持系统的简单性和稳定性。
