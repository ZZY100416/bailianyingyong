# 聊天机器人记忆上下文功能

## 功能概述

为聊天机器人添加了完整的记忆上下文功能，包括：

1. **会话管理** - 每个对话都有唯一的会话ID
2. **上下文记忆** - AI可以记住之前的对话内容
3. **历史查询** - 可以查看当前会话的历史消息
4. **会话控制** - 可以创建新会话或清除历史记录

## 技术实现

### 后端改进

1. **Message实体扩展**
   - 添加了`sessionId`字段用于会话管理
   - 支持按会话ID查询和删除消息

2. **API端点新增**
   - `POST /chat/stream` - 支持sessionId参数的流式对话
   - `GET /messages/session/{sessionId}` - 获取指定会话的历史消息
   - `DELETE /messages/session/{sessionId}` - 清除指定会话的历史
   - `POST /session/new` - 创建新的会话ID

3. **上下文构建**
   - 自动获取当前会话的历史消息（最多保留最近10轮对话）
   - 将历史消息作为上下文发送给AI模型
   - 支持连续对话的上下文理解

### 前端改进

1. **会话管理器**
   - 自动生成和管理会话ID
   - 支持会话的创建和清除

2. **UI增强**
   - 添加了"查看历史"、"新会话"、"清除历史"按钮
   - 实现了会话历史弹窗显示
   - 支持实时会话管理

3. **上下文传递**
   - 每次对话都会携带当前会话ID
   - 确保AI能够访问完整的对话历史

## 使用方法

### 1. 数据库迁移

首先需要执行数据库迁移脚本：

```sql
-- 在MySQL数据库中执行
ALTER TABLE messages ADD COLUMN session_id VARCHAR(255);
CREATE INDEX idx_messages_session_id ON messages(session_id);
CREATE INDEX idx_messages_created_at ON messages(created_at);
CREATE INDEX idx_messages_session_created ON messages(session_id, created_at);
```

### 2. 启动服务

1. 启动后端服务（端口5649）
2. 启动前端服务（端口5173）

### 3. 功能使用

- **正常对话**：直接输入消息，AI会记住之前的对话内容
- **查看历史**：点击"查看历史"按钮查看当前会话的所有消息
- **新会话**：点击"新会话"按钮开始一个全新的对话
- **清除历史**：点击"清除历史"按钮删除当前会话的所有历史记录

## 技术特点

1. **智能上下文管理**
   - 自动限制上下文长度（最多20条消息）
   - 保持对话的连贯性和相关性

2. **性能优化**
   - 数据库索引优化查询性能
   - 流式响应保证用户体验

3. **用户友好**
   - 直观的UI界面
   - 简单的操作流程

4. **数据安全**
   - 会话隔离，不同会话互不影响
   - 支持历史数据的完全清除

## 注意事项

1. 确保数据库已正确迁移
2. 会话ID在浏览器刷新后会重新生成
3. 历史消息按时间顺序显示
4. 清除历史操作不可恢复，请谨慎使用

## 扩展功能

未来可以考虑添加的功能：

1. 会话列表管理
2. 会话搜索功能
3. 会话导出功能
4. 多用户会话隔离
5. 会话标签和分类
